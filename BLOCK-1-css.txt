BLOCK 1 of 3  —  CSS
WHERE IT GOES: in index.html, paste on the line immediately BEFORE
the closing </style> tag in the <head>. (Search for </style>.)
Copy everything below the dashed line.
------------------------------------------------------------------

  /* ---- tossed badge hero ---------------------------------- */
  body{overflow-x:clip}                 /* the card flies in from off-column */

  .tbc-stage{
    position:relative;height:360px;margin:0 0 30px;
    touch-action:none;user-select:none;-webkit-user-select:none;
  }
  @media(min-width:900px){
    header{display:grid;grid-template-columns:1fr 300px;column-gap:46px;align-items:center}
    header > *{grid-column:1}
    .tbc-stage{grid-column:2;grid-row:1/-1;height:100%;min-height:430px;margin:0}
  }

  .tbc-shadow{
    position:absolute;top:0;left:0;transform-origin:50% 50%;
    background:#16202A;border-radius:12px;will-change:transform,opacity,filter;
  }
  .tbc-card{
    position:absolute;top:0;left:0;transform-origin:50% 50%;z-index:2;
    border-radius:12px;background:#FAFAF8;border:1px solid var(--rule);
    overflow:hidden;cursor:grab;will-change:transform;
  }
  .tbc-card.is-held{cursor:grabbing}
  .tbc-card:focus-visible{outline:2px solid var(--accent);outline-offset:4px}

  .tbc-pass{display:flex;flex-direction:column;height:100%;padding:13px 13px 11px;position:relative}
  .tbc-slot{
    position:absolute;top:8px;left:50%;translate:-50% 0;width:40px;height:6px;border-radius:3px;
    background:var(--rule);
  }
  .tbc-head{
    display:flex;justify-content:space-between;align-items:baseline;margin-top:11px;
    font-family:"Space Mono",monospace;font-size:9.5px;letter-spacing:.2em;text-transform:uppercase;
    color:var(--muted);padding-bottom:8px;border-bottom:1px solid var(--rule);
  }
  .tbc-portrait{
    position:relative;margin:11px 0 9px;aspect-ratio:1/0.94;overflow:hidden;border-radius:4px;
    background:var(--paper-2);display:grid;place-items:center;
  }
  .tbc-portrait img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;display:block;filter:grayscale(1) contrast(1.04)}
  .tbc-portrait .tbc-ph{font-family:"Space Mono",monospace;font-size:9px;letter-spacing:.3em;color:var(--muted);opacity:.6}
  .tbc-name{font-family:"Space Grotesk",sans-serif;font-weight:700;font-size:1.18rem;line-height:1.04;letter-spacing:-.02em;margin:0}
  .tbc-role{font-family:"Space Mono",monospace;font-size:9.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);margin:6px 0 0;line-height:1.4}
  /* the pivot line is in the accent, so it reads as added to the card
     rather than printed on it: what he is, then what he is here for */
  .tbc-aim{font-family:"Space Mono",monospace;font-size:9.5px;letter-spacing:.04em;color:var(--accent);margin:5px 0 0;line-height:1.4}
  .tbc-foot{
    margin-top:auto;padding-top:9px;border-top:1px solid var(--rule);
    display:flex;justify-content:space-between;align-items:flex-end;
    font-family:"Space Mono",monospace;font-size:9px;letter-spacing:.12em;color:var(--muted);
  }
  .tbc-stamp{
    position:absolute;right:9px;bottom:9px;font-family:"Space Mono",monospace;
    font-size:8.5px;line-height:1.35;letter-spacing:.22em;text-align:center;
    color:var(--accent);border:1.5px solid var(--accent);padding:4px 6px 3px;border-radius:2px;
    rotate:-7deg;opacity:.9;
  }
  .tbc-guilloche{position:absolute;inset:0;opacity:.14;pointer-events:none}
  .tbc-hint{
    position:absolute;left:50%;bottom:-4px;translate:-50% 0;z-index:3;
    font-family:"Space Mono",monospace;font-size:9.5px;letter-spacing:.2em;text-transform:uppercase;
    color:var(--muted);white-space:nowrap;transition:opacity .5s ease;pointer-events:none;
  }
  .tbc-hint.is-gone{opacity:0}
